global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/openjarvis-alerts.yml

scrape_configs:
  - job_name: openjarvis
    metrics_path: /metrics
    scheme: https
    static_configs:
      - targets:
          - ${OPENJARVIS_DOMAIN}

